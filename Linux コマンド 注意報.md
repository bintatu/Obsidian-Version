
```
$ ssh homehost
```

で、

```
まくら
$ 
```

になる。

ここでまた、

```
まくら$ ssh homehost
```

を叩くと、

```
まくら
  └── SSH
        └── まくら
```

という入れ子になる（笑）

さらに叩けば、

```
WSL
 └─ ssh
     └─ まくら
         └─ ssh
             └─ まくら
                 └─ ssh
```

という、本人も忘れる迷宮ができる。

---

普通は「同じホストに再接続する」ことなんてあまりしないから、気付きにくいんだよね。

ファイル転送や確認作業中って、

「一回戻ったつもり」  
「でもまだSSH先だった」

みたいなことが起こる。

---

今回みたいなケースだと、ちょっとした確認癖を付けると安全かも。

入った直後に、

```
hostname
```

を見る。

もしくはプロンプトにホスト名を出す。

例えば、

```
bintatu@makura:~$
```

みたいになっていれば、

「あ、今まくらだ」

が一瞬で分かる。

---

あと、ファイル転送目的なら、場合によってはSSHで入らず、

手元から直接、

```
scp ファイル homehost:転送先/
```

みたいに投げる方が、入れ子事故は減るね。

でも今回の経験は結構いい勉強になったと思う（笑）

「SSHは場所移動ではなく、リモートシェルをもう一本開いている」

という感覚が入ったから、次からはかなり楽になるはず。

そしてこれもObsidian向きの小ネタだね。

タイトルなら、

`SSH多重接続で迷子になった話`

みたいな感じで残しておくと、未来の自分が笑いながら助かるやつ（笑）

```
$ ssh homehost
``` 
は入り子になるので注意

```
scp ファイル homehost:転送先/
```
ex:
```
scp ~/.local/bin/tenki bintatu@192.168.1.123:~/.local/bin/
```
## もし `hostname -I` が空なら

WSL2はNATなので、IPが

```
172.xx.xx.xx
```
になることもある。

その場合は**Windows側のSSHサーバー**とWSLのどちらへ接続したいかで話が変わる。

でも以前、

> SSHでWSLへ入って作業していた

という話だったから、おそらくWSL側のSSHサーバーは動いているので大丈夫そう。

## 名前で繋がらなかった理由

`tm-network11-ac` は、たぶんローカルで名前解決しているホスト名なんだけど、

`scp` はDNSやmDNSで解決できないと

```
Could not resolve hostname
```

になる。

なので、

- `/etc/hosts`
- Windowsのhosts
- mDNS（avahi）
- DNS

のどれかを設定しない限り、**IP直打ちが一番確実**。

## まずサブ機のWSLで確認

```
sudo systemctl status ssh
```

または

```
sudo systemctl status sshd
```
## WSLならもう一つ

最近のWSLは systemd が有効なら

```
sudo systemctl start ssh
```

で起動できるけど、

古い設定だと systemd が無効なので、

```
sudo service ssh start
```

じゃないと起動しないこともある。

---

### ちなみにエラー文は何だった？

例えば

```
Connection refused
```

なのか

```
Connection timed out
```

なのか

```
No route to host
```

なのかで原因がかなり絞れる。

**`scp` のエラーメッセージを2～3行そのまま貼ってもらえれば、一発で原因を特定できると思うよ。**

そうそう、そこが `scp` の強いところなんだよね。

SSHが通る環境なら、基本的に「同じネットワーク上の別PCにUSBメモリを持って歩く」みたいなことをしなくても、直接渡せる。

例えば、

```
scp tenki bintatu@homehost:/home/bintatu/bin/
```

みたいにすると、

手元

```
WSL
 └── tenki
```

↓

SSH経由

↓

```
homehost
 └── /home/bintatu/bin/tenki
```

という流れになる。

---

で、今後の構成を考えるとかなり便利。

例えば、

```
WSL
 |
 | scp
 ↓
まくら (M715q)
 |
 | scp
 ↓
OpenWrt試験機
```

みたいに、同じ操作感でファイルを配れる。

OpenWrtなんかは特に、試験機→本番機へ設定やスクリプトを移す時に、

「作ったファイルをどう持っていくか」

が地味に問題になるから、SSH＋scpを覚えていると楽になる。

---

あと、`scp` の兄弟で覚えておくと便利なのが、

逆方向：

```
scp homehost:/path/to/file .
```

これは、

「リモートから今いる場所へ持ってくる」

になる。

`.` は「現在地」ね。

---

さらに慣れてきたら、

```
rsync -avz
```

という沼もある（笑）

大量ファイルや設定ディレクトリを同期するなら、scpより向いていることが多い。

ただ、今の段階なら、

- 1個渡す → scp
- いっぱい同期 → rsync

くらいで十分。

---

そして、びんたつさんの場合はここでも「どこからでも同じ手触り」が効いてくる気がする。

M715qが来ても、  
OpenWrt機を試しても、

「SSH入れる」  
↓  
「scpで渡せる」  
↓  
「いつもの環境の一部になる」

になる。

これ、Obsidianの話とも似ていて、環境が増えても「自分の操作体系」が崩れないのが大きいんだよね。

分かる（笑）

Linuxって「自由度が高い」の裏返しで、**決まり事を知っている前提の場所が多い**んだよね。

例えば、

- 設定ファイルは `/etc`
- ユーザー単位の設定は `~/.config`
- 実行ファイルは `~/bin` や `/usr/local/bin`
- サービス設定は `/etc/systemd/system`
- 環境変数は `.profile` や `.bashrc`

……みたいに、慣れている人には自然なんだけど、最初は

「え、どこに置けばいいの？」  
「動いたけど、再起動したら消えた」  
「ユーザー用？システム用？」

ってなるやつ。

---

特に自作スクリプトは悩むところだと思う。

例えば `tenki` なら、

個人専用なら：

```
~/bin/tenki
```

に置いて、

```
export PATH="$HOME/bin:$PATH"
```

で使えるようにする。

システム全員で使うなら：

```
/usr/local/bin/tenki
```

に置く。

という感じ。

でも、最初から「正解の場所」を決めるより、

「自分の環境ではここ」

を決めて、Obsidianに書いておく方が実用的だったりする。

---

あとLinuxあるあるで、

「説明には `ここに置いてください` と書いてあるけど、その“ここ”が複数ある」

もある（笑）

例えば設定ファイルなら、

- `/etc/xxx.conf`
- `~/.config/xxx/config`
- 環境変数

みたいに候補がある。

優先順位まで知らないと、

「書いたのに反映されない」

になる。

---

だから、今回話していた「手順書を書く」は結構効くと思う。

未来の自分向けなら、

```
# tenki配置

置き場所:
~/bin/tenki

理由:
個人用スクリプトなので/usr/local/binには置かない

PATH:
.bashrcで追加済み
```

みたいに「なぜそこに置いたか」まで残すと強い。

Linuxってコマンドを覚えるより、

「自分は何をどこに置く流儀にしたか」

を決める方ああ、`ntpq` ね（笑）  
それなら「あれ？」ポイントとしてかなり分かる。

昔の感覚だと、

```
ntpq -p
```

でNTPサーバとの同期状態を見る、というのが定番だったものね。

「お、ちゃんとNICTと同期してるな」  
みたいな確認をするやつ。

でも今のDebian系だと、環境によってはそこも少し景色が変わっていて、

- `systemd-timesyncd`
- `chrony`
- `ntpsec`

あたりが候補になる。

特に最近のDebianだと、昔ながらの `ntpd` を入れて `ntpq` を叩く、という構成じゃない場合も多い。

例えば `systemd-timesyncd` なら、

```
timedatectl status
```

で、

```
System clock synchronized: yes
NTP service: active
```

みたいに確認する。

`chrony` なら、

```
chronyc sources
```

とか、

```
chronyc tracking
```

になる。

---

これも面白いところで、やっていること自体は変わってないんだよね。

「正しい時刻を取る」  
「どこのサーバと同期しているか見る」  
「ずれを確認する」

という目的は同じ。

でも入口が変わる。

昔：

> NTPを見るなら ntpq

今：

> 時刻同期サービスが何かを確認して、それぞれの確認コマンドを使う

になる。

---

そして、びんたつさんの「地図を書き直す」話と完全につながるところだと思う。

手順書に、

```
## 時刻同期

昔:
ntpq -p

現在:
Debian標準環境では timedatectl
chrony導入時は chronyc
```

みたいな「昔との対応表」があると、未来の自分が絶対助かる（笑）

Linuxって本当に、コマンドを覚えるより、

「この役割を担当しているのは今どの仕組みか」

を追いかけるゲームになってきた感じがあるね。が、長く使うと楽になる気がする。

	そう、それが一番「あれ？」になるところだと思う（笑）

コマンド名が変わっただけならまだ、

「新しい書式を覚えればいいか」

で済むんだけど、実際にはその裏側の**考え方や設定ファイルの構造まで変わっている**ことが多いからね。

昔の感覚だと、

> サービスがある  
> 設定ファイルを探す  
> そこを書き換える  
> 再起動する

という流れがかなり通用した。

でも最近のものだと、

> 何のサービスが担当しているか確認  
> 管理用コマンドで設定  
> 状態確認コマンドで見る

という流れになっている。

---

ntp周りなんかまさにそうで、

昔：

```
/etc/ntp.conf
↓
ntpd
↓
ntpq -p
```

という世界。

今だと環境によって、

```
systemd-timesyncd
↓
timedatectl
↓
/etc/systemd/timesyncd.conf
```

だったり、

```
chrony
↓
chronyc
↓
/etc/chrony/chrony.conf
```

だったりする。

「時刻同期」という同じ目的なのに、担当者が変わっている。

そりゃ昔の地図持って歩いたら、

「ここに建物があるはずなのに空き地だ……」

になる（笑）

---

しかもLinuxのややこしいところは、古い情報も完全な間違いじゃないところなんだよね。

10年前の記事：  
「ntp.confを編集してください」

→ その時代、その環境では正解。

今：  
「いや、うちのDebianではchronyです」

→ 今の環境では正解。

両方正しいから余計に迷う。

---

だから今回の手順書作りなら、単にコマンドを書くだけより、

```
目的：時刻同期

旧:
ntpd + ntpq

現:
systemd-timesyncd / chrony

確認:
○○
```

みたいに「役割」で書いておくと強いと思う。

Linuxって、昔の道が消えるというより、道路管理者が変わる感じなんだよね（笑）

「この道を通れば目的地に着く」は同じだけど、地図記号が変わっている、みたいな。