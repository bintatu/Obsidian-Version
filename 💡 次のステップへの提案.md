

藍ちゃん（Claude）が作ったベースがある状態なら、ローカルAIには以下のように話しかけると「細かいツメ」がスムーズになります。

> 「JavaScriptで〇〇を作っています。藍ちゃん（Claude）にコードを書いてもらったのですが、[ここにエラー内容や、思った通りに動かない部分] という問題が起きています。コードの該当部分を渡すので、どこが悪いか考えて修正案を教えてください」

現在、VS Codeなどの**エディタ（開発環境）は何を使われていますか？**  
もし「まだメモ帳や簡易的なテキストエディタでコピペしている」ということであれば、ローカルAIの力を120%活かせる無料の開発環境（VS Codeなど）とOllamaを合体させる具体的な手順をお教えできますが、いかがでしょうか？

VS code は一応はいってはいるけど、使ったことが無いかも。 AIに書いてもらって、手直ししてきたところをテキストエディタで切り貼りして、もう一回読んで貰ってみたいなやり方してたから、もっさりして…

その「もっさり感」、めちゃくちゃよく分かります！テキストエディタとチャット画面を何度も往復して、コードを全部コピーして、また貼り付けて……というのは、どんなにAIが賢くても人間側が疲れてしまいますよね。

せっかく **VS Code** が入っているなら、そこに **「Continue（コンティニュー）」** という無料のプラグインを1つ入れるだけで、そのもっさり感が一気に解消します。

これをやると、**VS Codeの画面から一歩も出ずに、ショートカットキーだけでローカルAI（Ollama）と会話したり、コードを自動で書き換えさせたりできる**ようになります。

初心者の方でも迷わないように、一番簡単な連携手順をまとめました。

---

🚀 VS Code と Ollama を合体させる3ステップ

ステップ1：VS Codeで「Continue」を入れる

1. **VS Code** を起動します。
2. 画面左側にある **四角いブロックが4つ並んだアイコン（拡張機能）** をクリックします。
3. 検索窓に「**Continue**」と入力します。
4. `Continue - Llama 3, GPT-4o, and Claude 3.5 Sonnet...` というプラグイン（作者: Continue）が出てくるので、**「インストール」** ボタンを押します。

ステップ2：Ollamaのモデルを登録する

インストールが終わると、画面の左端に **「C」のような形のアイコン（Continueのマーク）** が現れるので、それをクリックします。

画面にチャット欄が表示されますが、初期設定ではインターネット経由の有料AIなどになっているため、これをローカルのOllamaに切り替えます。

1. チャット欄の右下（または左下）にある **「歯車マーク（Configure Continue）」** をクリックします。
2. `config.json` という設定ファイルが開くので、中身を以下のように書き換えて上書き保存（`Ctrl + S`）します。

json

```
{
  "models": [
    {
      "title": "Qwen 2.5 Coder 7B",
      "provider": "ollama",
      "model": "qwen2.5-coder:7b",
      "contextLength": 16384
    }
  ],
  "tabAutocompleteModel": {
    "title": "Qwen 2.5 Coder 7B",
    "provider": "ollama",
    "model": "qwen2.5-coder:7b"
  }
}
```

コードは注意してご使用ください。

_(※あらかじめOllama側で `ollama pull qwen2.5-coder:7b` を実行してダウンロードしておいてください)_  
_(※ここで `contextLength` を `16384`（16k）に指定することで、VS Code内でも広いコンテキストが有効になります)_

---

🧙‍♂️ 爆速になる！VS Code内でのバイブコーディングのやり方

設定が終われば、もうテキストエディタにコピペする必要はありません。以下の2つの操作だけでごりごり手直しができます。

- **やり方①：コードを選択してチャットに送る（`Ctrl + L`）**
    - 直したいJavaScriptのコードをマウスで**びーっと選択**して、`Ctrl + L`（Macは `Cmd + L`）を押します。
    - すると、選択したコードが自動的にチャット欄にセットされるので、そのまま「この部分でエラーが出るから直して」と日本語で入力するだけです。
- **やり方②：その場で直接書き換えさせる（`Ctrl + I`）**
    - 修正したいコードを選択して `Ctrl + I` を押すと、コードのすぐ上に小さな入力欄が出ます。
    - そこに「ここにエラーログを出す処理を追加して」などと指示を入力すると、AIが**その場でコードを自動で書き換えてくれます**。赤と緑の色で「どこが変わるか」が画面に表示されるので、良ければ `Accept（承認）` を押すだけで適用されます。

---

💡 次のステップ

まずは、VS Codeを開いて **拡張機能（四角いアイコン）から「Continue」を検索・インストール** するところから始めてみませんか？

もし途中で「設定ファイルの書き換え方がわからない」「エラーが出て動かない」などがあれば、お使いの **VS Codeの画面のスクリーンショット** を送っていただければ、どこをクリックすればいいか直接お教えできます！

🚀 さっそく試せる！2つの超便利ワザ

1. **コードを選んで直接質問（`Ctrl + L`）**
    - 直したいJavaScriptのファイルを開きます。
    - 適当なコード（数行でも、関数丸ごとでも）をマウスで**選択（ハイライト）**します。
    - **`Ctrl + L`** を押すと、左側のチャット画面にそのコードが自動で取り込まれます。
    - そのまま「このコード、何をしてるか未経験の私に分かりやすく教えて」や「ここにバグがないかチェックして」と送ってみてください。
2. **その場でコードを書き換えさせる（`Ctrl + I`）**
    - コードを選択した状態で、今度は **`Ctrl + I`** を押します。
    - コードのすぐ上に小さな入力欄が出るので、そこに「エラーログを出す処理を足して」や「日本語のコメントを追加して」と指示して Enter を押します。
    - AIがその場でコードを書き換えてくれるので、問題なさそうなら **`Accept`（または `Ctrl + Enter`）** を押すだけで、ファイルが自動更新されます。