

## 1. 発生事象 Windows環境において、Tera Term 5.6.2を使用し、自宅サーバーへSSH秘密鍵ログインを試みた際、以下のエラーが発生して接続に失敗する。 
* **エラーメッセージ**: `Read error SSH2 private key file`
-   **詳細コード**: `error:09FFF06C:PEM routines:CRYPTO_internal:no Start line` 
  
    ## 2. 前提条件と環境
  
   * **接続元**: Windows PC (Tera Term 5.6.2) 
   * **接続先**: 自宅サーバー (Linux) 
     * **他ツールの状況**: * Windows標準の「Terminal (PowerShell/OpenSSH)」からは問題なく鍵ログインが可能。 
     * 「Termius」からも秘密鍵およびSFTP接続が100%正常に動作する。 * `~/.ssh/config` の `IdentityFile` で指定された本物の秘密鍵を使用。
     * サーバー側のフォルダ権限 (`~/.ssh` 700 / `authorized_keys` 600) は正常。 * Tera Termからの「プレーンパスワード（パスワード認証）」によるログインは正常に通る。 
## 3. 実施した検証内容と結果 Windowsのアクセス権限（リードオンリー）や、各種ファイル汚染（BOM・改行コード）を疑い、以下の徹底した切り分けを実施。
 1. **既存ファイルの移動と権限チェック** * Windows側の `.ssh` フォルダにシステム・他アプリによるリードオンリー（ロック）がかかっていたため、Cドライブ直下に新規フォルダ（`C:\SSH_KEY` など）を作成し隔離。
     * 結果：改善せず（`Read error` のまま）。 
    2. **サーバー内実体からの完全クリーンコペ** * 正常にアクセスできるTerminal経由で、サーバー上の本物である秘密鍵を `cat` 表示。 
       * 余計な空行やスペースを排除し、Windowsのメモ帳（UTF-8/拡張子なし）に新規貼り付け・保存して指定。
       * 結果：改善せず（`no Start line` のまま）。 
       2. **Tera Term純正鍵生成機能による検証（決定的要因）** 
          * アプリや暗号形式の相性を排除するため、Tera Term自身の機能（設定 ＞ SSH鍵生成）を使用。
          * 「ED25519」、かつパスフレーズを完全に「空欄（なし）」という、最もエラーが起き得ない純粋な状態で新規鍵ペアを出力。
          * 結果：**Tera Term自身が生成したその鍵ファイルすら、指定時に全く同じエラーを吐き出す状態。**

## 4. 最終結論と原因分析 原因はファイル、権限、およびサーバー側の設定ではなく、**「Tera Term 5.x（内部のOpenSSL 3.x）と特定のWindows環境下における、ファイル読み込みの不整合（システムバグ）」** と断定。 

 * 秘密鍵をエクスプローラーから読み込んでメモリに展開する段階で、先頭のヘッダー文字列（`-----BEGIN...`）を正しくパースできず、データ破損として誤判定している。
 * ユーザーの手順や設定は200%完璧。アプリ側の固有バグのため、これ以上の設定変更によるアプローチは不可能。
## 5. 今後の方針

 * **メイン環境**: 完璧に動作している「Termius」および「Windows Terminal」による鍵認証ログインを継続運用する。 

* **バックアップ体制**: ネットワークやアカウント自体は正常なため、万が一の非常用窓口として、Tera Termからは「プレーンパスワード認証」でのアクセス手段が残されている状態とし、現状でクローズとする。