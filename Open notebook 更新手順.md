#PC #Tips

⚡ アップデートの基本手順

ターミナルを開き、`docker-compose.yml` があるディレクトリに移動して以下のコマンドを順に実行してください。

1. **コンテナの一時停止**
    
    bash
    
    ```bash
    docker compose down
    ```
    
    コードは注意してご使用ください。
    
2. **最新イメージの取得**
    
    bash
    
    ```bash
    docker compose pull
    ```
    
    コードは注意してご使用ください。
    
3. **コンテナの再起動（バックグラウンド実行）**
    
    bash
    
    ```bash
    docker compose up -d
    ```
    
    コードは注意してご使用ください。
    

---

⚠️ 注意点と確認事項

- **古いイメージの削除**: アップデート後、古いイメージがディスク容量を圧迫することがあります。不要な場合は `docker image prune` で削除できます。
- **環境変数の確認**: 大幅なアップデートの場合、`.env` ファイル（環境変数設定）に新しい項目が追加されている場合があります。もし起動しない場合は、公式リポジトリの最新の `.env.example` と差分がないか確認してください。

無事にブラウザ上の更新通知が消え、最新版が起動したかご確認ください。