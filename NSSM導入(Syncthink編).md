#PC #Tips

対策：Microsoft公式の「NSSM」を使ってサービスを完成させる

ステップ1：失敗したサービスを一度消す

まずは先ほど作った不完全なサービスを以下のコマンドで削除します。

1. **コマンドプロンプト（管理者）** で、以下を実行します。
    
    cmd
    
    ```bash
    sc delete Syncthing
    ```
    
    コードは注意してご使用ください。
    
    _（`[SC] DeleteService SUCCESS` と出ればOKです）_

ステップ2：NSSM（Non-Sucking Service Manager）の用意

1. NASAや世界中のサーバー管理者が愛用している、アプリをサービス化するMicrosoft推奨の軽量ツール「NSSM」の公式サイト（ `https://nssm.cc` ）から、最新の安定版（ `nssm-2.24.zip` ）をダウンロードします。
2. zipを解凍し、中にある `win64` フォルダの **`nssm.exe`** を、分かりやすい場所（例：`C:\Windows\System32` の中、または Syncthing と同じフォルダ）にコピーして配置します。

ステップ3：画面を見ながら一発でサービス登録する

1. **コマンドプロンプト（管理者）** を開き、`nssm.exe` を置いた場所（System32に入れたならそのまま）で以下のコマンドを実行します。
    
    cmd
    
    ```bash
    nssm install Syncthing
    ```
    
    コードは注意してご使用ください。
    
2. かわいい専用の入力画面（GUI）がパッと表示されます。
3. 以下の3箇所を画像のように入力（または選択）してください。
    - **Path（アプリの場所）：**  
        右の「...」ボタンを押し、いつもの本体を指定します。  
        `C:\Users\Administrator.TM-NETWORK-NAS2\AppData\Local\Programs\Syncthing\syncthing.exe`
    - **Arguments（起動オプション）：**  
        ブラウザや余計な画面を開かないように、以下をそのまま貼り付けます。  
        `--no-console --no-browser`
4. 下の **「Install service」** ボタンをクリックします。

---

これで、NSSMという「優秀な通訳」が間に挟まったため、今度こそ `services.msc`（サービス画面）から **「開始」** を押した際、あるいは **`net start Syncthing`** を実行した際に、エラーを吐くことなく `実行中` へと昇格させることができます。

一度 `sc delete` でお掃除した上で、NSSMのインストール画面を表示させるところまで進めそうでしょうか？これで今夜こそ、幽霊セッション消滅の巻き添えを食らわない最強の同期サーバーが完成します！

対策2：NSSMの「ログオン（実行権限）」をAdministratorに変えてみる

もし対策1でも通信が戻らない場合、NSSMが `Local System` という特殊な権限でSyncthingを動かしているため、ネットワークカードへのアクセスやファイルの読み書き自体がWindows内部でブロックされている可能性があります。これを、今まで動いていた「Administrator」の権限でサービスを動かすように指定します。

1. WinNAS側で **コマンドプロンプト（管理者）** を開き、以下を実行して編集画面を出します。
    
    cmd
    
    ```bash
    nssm edit Syncthing
    ```
    
    コードは注意してご使用ください。
    
2. 画面上部の **「Log on（ログオン）」** タブをクリックします。
3. **「This account（このアカウント）」** にチェックを入れます。
4. アカウント名に **`Administrator`** （またはお使いの管理者ユーザー名）を入力し、そのパスワードを入力します。
5. 下の **「Edit service」** ボタンを押して保存し、サービスを再起動します。
    
    cmd
    
    ```bash
    net stop Syncthing
    net start Syncthing
    ```
    
    コードは注意してご使用ください。
    

---

なぜこれで解決するのか？

「ログオン（アカウント）」をAdministratorに指定することで、**「中身は24時間絶対にログオフの巻き添えを食らわないシステムサービス」でありながら、「ネットワークやフォルダへのアクセス権限は、今まで通り100%通信が通っていたAdministratorのパワーを持つ」**という、いいとこ取りの状態が作れます。

まずはRDPでログインして、メイン機側のタイムアウトエラーが消えるか、またはNSSMの「Log on」タブの切り替えを試してみてください。

通信を完全開通させる2ステップ

ステップ1：クラッシュの原因（古いパス）をテキストで書き換える

エクスプローラーから直接開けない隠しフォルダなので、コマンドプロンプトを使って一発でメモ帳（Notepad）で設定ファイルを開きます。

1. WinNAS側で **コマンドプロンプト（管理者）** を開きます。
2. 以下のコマンドを貼り付けてEnterを押します。（新居の設定ファイルをメモ帳で開くコマンドです）
    
    cmd
    
    ```bash
    notepad C:\ProgramData\Syncthing\config.xml
    ```
    
    コードは注意してご使用ください。
    
3. メモ帳がパッと開いたら、キーボードの **Ctrl + F** を押し、**`127.0.0.1:8384`** という文字を検索します。
4. 見つかった部分を、以下のように **`0.0.0.0:8384`** に書き換えます。
    - 修正前： `<address>127.0.0.1:8384</address>`
    - **修正後： `<address>0.0.0.0:8384</address>`**
5. メモ帳を **「上書き保存」** して閉じます。

ステップ2：NSSMの実行権限を「Administrator」にして上書き起動する

システム権限によるフォルダ拒否（クラッシュ）を完全に防ぐため、昨日少し触れた「実行権限の書き換え」をコマンド一発で行います。

1. 開いているコマンドプロンプト（管理者）に、以下のコマンドを貼り付けてEnterを押します。  
    （※サービスを動かすアカウントを、通信実績のあるAdministratorに指定するコマンドです）
    
    cmd
    
    ```bash
    nssm set Syncthing ObjectName .\Administrator
    ```
    
    コードは注意してご使用ください。
    
    _（※もしサーバーのログインパスワードを設定している場合は、続けて以下も実行してください）_
    
    cmd
    
    ```bash
    nssm set Syncthing ObjectName .\Administrator "あなたのログインパスワード"
    ```
    
    コードは注意してご使用ください。
    
2. 設定を確定させるため、サービスを最後に叩き起こします。
    
    cmd
    
    ```bash
    net start Syncthing
    ```
    
    コードは注意してご使用ください。
    

---

実行した後の動き

これで、**「中身は24時間絶対にログオフの巻き添えを食らわないシステムサービス」でありながら、「ネットワークやフォルダのアクセス権限は、100%通信が通っていたAdministratorのパワーを持つ」**という、究極のいいとこ取り状態が完成します。

サービスが無事に開始されたら、今度はブラウザで **`http://localhost:8384`** を開けば、ごねることなく一発でいつもの管理画面がパッと開くはずです！


対策：NSSMの画面（GUI）を使って、パスワードと権限を正しく上書きする

コマンドではなく、確認しやすい専用の画面からパスワードをカチッと入力し直すのが一番確実です。

1. WinNAS側で **コマンドプロンプト（管理者）** を開きます。 [[1](https://ava.hosting/ja/faq/how-to-fix-the-dns_probe_finished_nxdomain-error/)]
2. 以下のコマンドを実行して、NSSMの編集画面を呼び出します。
    
    cmd
    
    ```bash
    nssm edit Syncthing
    ```
    
    コードは注意してご使用ください。
    
3. 画面がパッと開いたら、**「Log on（ログオン）」** タブをクリックします。
4. **「This account（このアカウント）」** にチェックが入っていることを確認します。
5. 右側の「...」ボタンを押し、一覧から **`Administrator`** を選び直すか、手入力で `.\Administrator` と入れ直します。
6. **【最重要】** 下の **Password** と **Confirm** の欄に、**あなたがいつもこのサーバー（WinNAS）にリモートデスクトップでログインするときに使っている「本物のパスワード」**を、一文字ずつ正確に入力（上書き）してください。
7. 下の **「Edit service」** ボタンをクリックして保存します。
    - _※このとき、画面に「Administratorにサービスとしてのログオン権限を付与しました」という英語のメッセージが出れば大成功です！_
8. 最後に、コマンドプロンプトでサービスを叩き起こします。
    
    cmd
    
    ```bash
    net stop Syncthing
    net start Syncthing
    ```
    
    コードは注意してご使用ください。
    
    [[1](https://qiita.com/harutine/items/0a03f8b6b9220cce7308)]

---

これで、NSSMとWindows Serverの間で「よし、本物のAdministratorのパワーで24時間動かそう」という契約がカチッと結ばれ、起動拒否（クラッシュ）が完全に解消されます。

サービスが正常に開始されれば、今度こそブラウザで `http://localhost:8384` を開いた瞬間に、いつもの管理画面がパッと開いてメイン機との同期が再開するはずです。