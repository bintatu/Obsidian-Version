
1. サーバー側の合言葉（場所と権限）

- **場所:** `~/.ssh/authorized_keys`（フォルダではなく**ファイル**）
- **権限（最重要）:**
    - フォルダは `700` (`chmod 700 ~/.ssh`)
    - ファイルは `600` (`chmod 600 ~/.ssh/authorized_keys`)

2. Tera Term で繋ぐときのルール

3. ホスト（IPアドレス）を入力して「OK」
4. ユーザー名を入力
5. **「認証方式」で「RSA/DSA/ECDSA/ED25519鍵を使う」にチェックを入れる**
6. 「秘密鍵」ボタンを押し、自分のパソコンにある**秘密鍵ファイル**を選択
7. パスフレーズ（設定した場合）を入力して「OK」