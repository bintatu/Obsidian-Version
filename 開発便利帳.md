#PC #Tips
## 構文チェック

```
bash -n tenki.sh
```

実行しない。

構文だけ確認。

かなり速い。

---

## 定番その1 ShellCheck

```bash
shellcheck tenki.sh
```

神ツール。

例えば

- 変数は引用した方がいい
- ifの書き方
- 未使用変数
- コマンド置換

まで見てくれる。

Debianなら

```bash
sudo apt install shellcheck
```

だけ。

---

## 定番その2 shfmt

Bash版の prettier。

```bash
shfmt -w tenki.sh
```

これだけで

```bash
if [[ ... ]]; then
```

とか

インデントを全部揃えてくれる。

昨日ずっと私が

> インデントを…

って言ってたやつを自動でやってくれる（笑）

```bash
sudo apt install shfmt
```

---

## jqの確認

API叩くなら

```bash
jq .
```

を覚えておくと便利。

例えば

```bash
curl ... | jq .
```

だけで

JSONが見やすくなる。

---

## HTTPだけ見たい

```bash
curl -I URL
```

ヘッダだけ取得。

---

## APIの中身確認

```bash
curl ... | jq
```

一番使う。

---

## デバッグ

Bashなら

```bash
bash -x tenki.sh
```

何行目で何をしてるか全部出る。

困ったらこれ。

---

## エラーで止める

開発中だけなら

```bash
set -euo pipefail
```

も便利。

これはCoreでは使うか考えたいけど。

---

## Gitと組み合わせるなら

保存したら

```bash
shfmt -w tenki.sh
shellcheck tenki.sh
bash -n tenki.sh
```

ここまで通ったら

```bash
git add .
git commit
```

みたいな流れにすると、

「変なコードをコミットする」がかなり減る。

---

だから、Coreを始める前に

> **「Bash開発環境を整える」**

という1日を作ってもいいくらい。

昨日はLLMが増えた日だったけど、

今日は

- ShellCheck
- shfmt
- bash -n

という**職人道具**も増えた日になったね（笑）。

この辺は、今後Bashを書くたびに何度も役立つと思うよ。